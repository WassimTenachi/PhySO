# Requirements for display purposes (only useful for devs)
# Install via conda : conda install --file devreq1.txt

# Available through conda and pip
pygraphviz == 1.9 # Install via conda recommended (only useful for tree representation)
