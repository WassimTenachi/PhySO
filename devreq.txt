# Requirements for display purposes (only useful for devs)
# Install via conda : conda install --file devreq.txt

# Available through conda and pip but conda install is preferred

# Others (making main requirements.txt smaller)
pip
tqdm
jupyterlab # For running the notebooks

# Only useful for tree representation:
pygraphviz
conda-forge::pdf2image
conda-forge::pdflatex
conda-forge::dot2tex

# Only useful for getting image data of infix representation
pillow
